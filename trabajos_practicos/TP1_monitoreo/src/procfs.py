"""
Helpers para parsear /proc.  (se implementa en FASE 1)

Funciones previstas:
    listar_pids()              -> lista de PIDs (carpetas numéricas en /proc)
    leer_status(pid)           -> dict de /proc/<pid>/status
    leer_stat(pid)             -> dict de campos de /proc/<pid>/stat
    leer_cmdline(pid)          -> comando completo
"""
